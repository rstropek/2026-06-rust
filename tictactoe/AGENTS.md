## Quality Assurance Guideline

Whenever you write any code, make sure that it is

1. formated (`cargo fmt`),
2. warning free (`cargo check`),
3. Clippy warning free (`cargo clippy`),
4. running unit tests successfully (`cargo test`),
5. compiling (`cargo build`).
